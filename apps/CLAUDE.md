# apps/ — 共通ルール

**`apps/<app>/` を修正・追加・編集するときは必ず**、先に `writing-plans` スキルで計画を立て、その計画 md を**そのアプリの `docs/plans/YYYY-MM-DD-<feature>.md`** に保存してから実装する（`docs/plans/` が無ければ作る）。

各アプリ固有の規約は `apps/<app>/CLAUDE.md` を参照。
